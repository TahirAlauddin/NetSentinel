# Zabbix Agent 2 configuration for cache-server
Server=zabbix-server
ServerActive=zabbix-server
Hostname=cache-server

# Agent listens on default port
ListenPort=10050

# Log to stdout (Docker-friendly)
LogType=console

# Allow running remediation scripts via script.execute (test environment only)
AllowKey=system.run[*]
