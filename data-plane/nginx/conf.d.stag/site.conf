# Staging: switch HTTP vs HTTPS by changing the include line below.
# - netsentinel-http.conf: HTTP only (use when obtaining cert or running without SSL)
# - netsentinel-ssl.conf: HTTPS with redirect (use after certificates exist; edit YOUR_DOMAIN in that file)
include /etc/nginx/conf.d/includes/netsentinel-http.conf;
