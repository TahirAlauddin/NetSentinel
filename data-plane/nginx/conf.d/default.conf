# This file overrides nginx default config
# The actual configuration is in netsentinel.conf
# This minimal server block prevents conflicts
server {
    listen 80;
    server_name _;
    return 444;  # Close connection without response
}

